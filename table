#include<stdio.h>
int main()
{
	int i=1,n,a;
	printf("enter the value of a");
	scanf("%d",&a);
	printf("enter the value of n");
	scanf("%d",&n);
	do{
		printf("%d*%d=%d\n",a,i,i*a);
		++i;
}
while(i<=n);
return 0;
}
