#include<stdio.h>
int max(int a,int b)
{
    if(a>b)
    {
        return a;
    }
    else
    {
    return b;
    }
}


int main()
{
    int a,b,d;
    printf("enter a,b");
    scanf("%d%d",&a,&b);
    d=max(a,b);
    printf("max is%d",d);
}
