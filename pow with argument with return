#include<stdio.h>
#include<math.h>
int power(int base,int exp)
{
    int result;
    result=pow(base,exp);
    return result;
}
int main()
{
    int b,e,d;
    printf("enter the values of b,e");
    scanf("%d%d",&b,&e);
    d=power(b,e);
    printf("%d",d);
}
